name: post-sync-processing

# blog-contents から push される notion-sync-from-blog-contents PR が main に
# merge された後、auto-translate (ja → en) を実行して翻訳結果を新規 PR で開く。
# Notion fetch と R2 upload (image / video) は blog-contents 側
# (sync-blog-lacolaco workflow) が完了済なので、本 workflow では行わない。

on:
  pull_request:
    types: [closed]
    branches: [main]
  workflow_dispatch:

jobs:
  process:
    if: github.event_name == 'workflow_dispatch' || (github.event.pull_request.merged == true && github.event.pull_request.head.ref == 'notion-sync-from-blog-contents')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: setup pnpm
        uses: pnpm/action-setup@91ab88e2619ed1f46221f0ba42d1492c02baf788 # v6.0.6
        with:
          run_install: false
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .node-version
          cache: 'pnpm'

      - uses: actions/create-github-app-token@1b10c78c7865c340bc4f6099eb2f838309f1e8c3 # v3.1.1
        id: generate-token
        with:
          app-id: ${{ secrets.WORKER_APP_ID }}
          private-key: ${{ secrets.WORKER_APP_PRIVATE_KEY }}

      - run: pnpm install --frozen-lockfile
      - run: touch .env

      - name: Auto-translate ja → en for flagged articles
        run: pnpm run auto-translate
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

      - name: Create PR for translated content
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: ${{ steps.generate-token.outputs.token }}
          commit-message: 'chore: auto-translate (post-sync)'
          branch: post-sync-processing
          delete-branch: true
          title: 'chore: auto-translate (post-sync)'
          body: 'Automated post-sync from notion-sync-from-blog-contents PR merge'
