---
// Catch-allページ: 定義されていないすべてのルートをキャッチして404ページにリダイレクト
// Node.js standaloneアダプターでは404.astroが機能しないため、このcatch-allページで確実にリダイレクト

// サーバーサイドレンダリングを有効化（動的ルートのため必須）
export const prerender = false;

// 404ページにリダイレクト
return Astro.redirect('/404', 302);
---
