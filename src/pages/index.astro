---
import { getCollection } from 'astro:content';
import List from '../layouts/List.astro';

const collection = await getCollection('post');
---

<List posts={collection.map((post) => post.data)} />
