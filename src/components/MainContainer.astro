---
type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<main
  class="max-w-screen-lg mx-[1rem] lg:mx-auto px-[1rem] lg:px-[2rem] bg-white rounded-lg shadow-lg"
  class:list={className}
>
  <slot />
</main>
