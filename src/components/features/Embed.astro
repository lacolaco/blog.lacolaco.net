---
type Props = {
  src: string;
  width?: string;
};

const { src, width } = Astro.props;
---

<iframe class="common-iframe" src={src} width={width ?? '100%'}></iframe>

<style>
  iframe {
    border: none;
    aspect-ratio: 4/3;
    max-width: 720px;
  }
</style>
